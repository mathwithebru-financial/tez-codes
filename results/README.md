# Publishable Results

Only compact, non-sensitive result summaries are included.

Large prediction arrays, loss-series archives, model checkpoints, temporary SHAP chunks, and generated figures are excluded from the repository.
